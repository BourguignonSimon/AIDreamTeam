# LangGraph multi-agent stack
langgraph==0.2.73
langchain-core>=0.3.0,<0.4.0

# LLM gateway
litellm>=1.44.0,<2.0.0

# Runtime
python-dotenv>=1.0.0

# ── Audio Transcription Service ──────────────────────────────────────────────

# Transcription engine — faster-whisper uses CTranslate2 for ~4× GPU speedup
faster-whisper>=1.0.0,<2.0.0

# FastAPI stack
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9,<1.0.0   # required for UploadFile support

# Audio I/O
soundfile>=0.12.0,<1.0.0         # read WAV/FLAC/OGG without ffmpeg dependency
numpy>=1.26.0,<3.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0,<1.0.0             # FastAPI TestClient async backend
